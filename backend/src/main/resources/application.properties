# Nome da aplicação
spring.application.name=Kredix

# Banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/Kredix?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=tarefex
spring.datasource.password=tarefex
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update

# Servidor
server.port=8080
server.address=0.0.0.0

# Mensagens de log 
logging.level.org.springframework=INFO

# JWT
jwt.secret=kredix-super-secret-key-2024-must-be-256-bits!!